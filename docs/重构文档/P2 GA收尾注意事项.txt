“GA 后收尾”Punch List，按优先级归类，覆盖监控、清理、CI、文档与合规等，便于1–2周观察期内逐项关闭。

必做项（1–2 周内）

监控接入确认: 在生产注入平台 Provider
文件: frontend/src/main.js:VITE_SENTRY_DSN，frontend/src/infra/telemetry.providers.ts
验证: 事件入库，字段齐全；告警策略按阈值生效（参考 docs/重构文档/telemetry-dashboard.md）
阶段放量收口: 将 stage 放量提至 100% 并稳定观察
文件: frontend/src/config/FlagRollouts.ts（stage.percent → 100）
验证: 登录后 flag_switched 打点有且仅有一次，轻量 smoke 正常
必要告警与回滚演练
配置告警→触发演练→执行 applySafeRollback() 并恢复
文档/记录: docs/重构文档/P2-启动报告.md 补充演练记录
建议项（稳定后执行）

死代码清理（最低侵入，过观察期再删）
候选:
frontend/src/views/business/ticket/components/AssignDialog.vue
frontend/src/views/business/ticket/components/TicketForm.vue
清单: docs/重构文档/dead-code-candidates.md:1
验证: 全局检索无引用后删除，并保留一版最小回滚说明
文案与消息一致性
统一成功/失败提示文案（集中 frontend/src/views/business/ticket/template.util.ts）
规范错误码映射（确认后端码表，避免“返回结果异常”误判）
监控降噪与隐私
审查载荷字段避免 PII；必要时对 userId 做哈希或脱敏（在 Provider 层处理）
设置采样与限流（平台侧），避免峰值时噪音
CI 准入扩展
将以下 Job 加入必需检查：
template_telemetry_smoke（CI）
template_health_smoke（CI）
说明位置: .github/workflows/ci.yml:1
验证项（运行期健康）

事件完整性
submit/success/error/validate_false/cancel/busy/abort/flag_switched/enter_press 全部可见，字段齐全（env/userId/roles/durationMs/firstInvalidField）
性能与稳定
运营期检查 busy 占比 < 2%，平均耗时 < 1500ms、P95 < 3000ms（prod）
热切换无白屏/无异常日志；轻量 smoke 不发起真实写操作
覆盖与门禁
继续保持 npm run test:ci:p2、npm run test:ci:telemetry、npm run test:ci:health 全绿
覆盖率门禁脚本：frontend/scripts/coverage-check.mjs:1 持续通过
文档与移交（GA 收尾）

完成 GA 版文档
docs/重构文档/P2-启动报告.md:1 已新增 Stage 8；补齐“实际看板链接/告警规则/回滚演练记录”
维护指南：docs/重构文档/telemetry-dashboard.md:1（看板与告警），docs/重构文档/dead-code-candidates.md:1
进度追踪：docs/refactoring-progress.md:1 已记录 Stage 8 达成项